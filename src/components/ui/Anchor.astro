---
interface Props {
  href: string
  target?: '_blank' | '_self' | '_tab'
}

const { href, target } = Astro.props
---

<a
  class="text-secondary hover:!text-[#00B4FB]/80 hover:!underline-offset-5 hover:!underline transition-all"
  href={href}
  target={target || null}
>
  <slot />
</a>